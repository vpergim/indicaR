% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grafico_mapa_provincias.R
\name{grafico_mapa_provincias}
\alias{grafico_mapa_provincias}
\title{Mapa por provincias}
\usage{
grafico_mapa_provincias(
  datos,
  codigo = "cod_provincia",
  valor = "valor",
  titulo = NULL,
  titulo_leyenda = NULL,
  paleta = "PuBu",
  n_clases = 5L,
  accuracy = 0.1,
  canarias = c("linea", "recuadro", "ninguno"),
  incluir_ceuta_melilla = FALSE
)
}
\arguments{
\item{datos}{Un \code{data.frame} con los datos que se desean representar.}

\item{codigo}{Cadena de texto con el nombre de la columna que contiene el
código provincial. Por defecto, \code{"cod_provincia"}.}

\item{valor}{Cadena de texto con el nombre de la variable numérica que se
desea representar. Por defecto, \code{"valor"}.}

\item{titulo}{Título del gráfico. Por defecto, \code{NULL}.}

\item{titulo_leyenda}{Título de la leyenda. Por defecto, \code{NULL}.}

\item{paleta}{Nombre de una paleta secuencial de RColorBrewer. Por defecto,
\code{"PuBu"}.}

\item{n_clases}{Número de intervalos utilizados para clasificar los valores.
Por defecto, \code{5}.}

\item{accuracy}{Precisión de las etiquetas mostradas sobre el mapa.
Por defecto, \code{0.1}.}

\item{canarias}{Elemento utilizado para señalar la posición desplazada de
Canarias. Puede ser \code{"linea"}, \code{"recuadro"} o \code{"ninguno"}.}

\item{incluir_ceuta_melilla}{Si es \code{TRUE}, mantiene Ceuta y Melilla en el
mapa. Por defecto, \code{FALSE}.}
}
\value{
Un objeto de clase \code{ggplot}.
}
\description{
Representa valores por provincia mediante un mapa coroplético de España.
Los datos se relacionan con la cartografía mediante el código provincial.
}
\details{
Canarias se muestran desplazadas junto a la Península. El usuario puede
añadir una línea, un recuadro o ningún elemento auxiliar alrededor de ellas.

Cada provincia debe tener como máximo una observación. La función no agrega
ni resume observaciones automáticamente.
}
\examples{
datos_ejemplo <- data.frame(
  cod_provincia = sprintf("\%02d", 1:52),
  valor = seq(50, 90, length.out = 52)
)

p <- grafico_mapa_provincias(
  datos_ejemplo
)
p

\dontrun{
datos <- consultar_indicador(
  "ID_DEL_INDICADOR",
  anyo = 2024,
  tipo_territorio = "provincia"
)

grafico_mapa_provincias(datos)
}

}
