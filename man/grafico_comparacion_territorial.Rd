% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grafico_comparacion_territorial.R
\name{grafico_comparacion_territorial}
\alias{grafico_comparacion_territorial}
\title{Comparación territorial de un indicador}
\usage{
grafico_comparacion_territorial(
  datos,
  categoria = "territorio",
  valor = "valor",
  colores_destacados = NULL,
  color_resto = "gray50",
  titulo = NULL,
  subtitulo = NULL,
  accuracy = 0.1,
  porcentaje = TRUE,
  margen = 1.1,
  orden_desc = TRUE
)
}
\arguments{
\item{datos}{Un \code{data.frame} con los datos que se desean representar.}

\item{categoria}{Cadena de texto con el nombre de la variable categórica.
Por defecto, \code{"territorio"}.}

\item{valor}{Cadena de texto con el nombre de la variable numérica.
Por defecto, \code{"valor"}.}

\item{colores_destacados}{Vector nombrado opcional con colores para
categorías concretas. Los nombres deben coincidir con los valores de
\code{categoria}.}

\item{color_resto}{Color utilizado para las categorías no incluidas en
\code{colores_destacados}. Por defecto, \code{"gray50"}.}

\item{titulo}{Título del gráfico. Por defecto, \code{NULL}.}

\item{subtitulo}{Subtítulo del gráfico. Por defecto, \code{NULL}.}

\item{accuracy}{Precisión utilizada para formatear las etiquetas numéricas.
Se pasa a \code{\link[scales:label_number]{scales::label_number()}}. Por defecto, \code{0.1}.}

\item{porcentaje}{Si es \code{TRUE}, añade el símbolo \verb{\%} a las etiquetas de los
valores. Por defecto, \code{TRUE}.}

\item{margen}{Factor utilizado para ampliar el eje horizontal y dejar espacio
para las etiquetas. Por defecto, \code{1.1}.}

\item{orden_desc}{Si es \code{TRUE}, ordena las categorías de mayor a menor.
Si es \code{FALSE}, las ordena de menor a mayor.}
}
\value{
Un objeto de clase \code{ggplot}.
}
\description{
Genera un gráfico horizontal para comparar el valor de un indicador entre
distintas categorías, normalmente territorios, en un único período.
}
\details{
Permite destacar determinadas categorías mediante colores específicos y
ordenar los valores de mayor a menor o de menor a mayor.

Cada categoría debe aparecer una sola vez en \code{datos}. La función no agrega
ni resume observaciones automáticamente.
}
\examples{
datos_ejemplo <- data.frame(
  territorio = c(
    "Illes Balears",
    "Ceuta",
    "Extremadura",
    "Melilla",
    "Cataluña",
    "Andalucía",
    "País Vasco",
    "Navarra",
    "Aragón",
    "Cantabria",
    "Madrid",
    "Comunitat Valenciana",
    "Murcia",
    "España",
    "Castilla-La Mancha",
    "La Rioja",
    "Galicia",
    "Asturias",
    "Castilla y León",
    "Canarias"
  ),
  valor = c(
    82.9, 81.7, 79.5, 79.2, 78.6,
    78.3, 77.8, 77.6, 76.8, 76.5,
    75.8, 75.7, 75.6, 75.5, 74.8,
    73.3, 70.0, 68.0, 66.4, 64.6
  )
)

colores_ejemplo <- c(
  "Comunitat Valenciana" = "#000000",
  "España" = "#AA151B"
)

p <- grafico_comparacion_territorial(
  datos_ejemplo,
  colores_destacados = colores_ejemplo
)

p

p +
  ggplot2::labs(
    title = "Indicador de ejemplo",
    subtitle = "Comparación territorial",
    caption = "Fuente: elaboración propia."
  ) +
  ggplot2::theme(
    plot.title.position = "plot",
    plot.title = ggplot2::element_text(hjust = 0, face = "bold", size = 16),
    plot.subtitle = ggplot2::element_text(size = 12),
    plot.caption = ggplot2::element_text(face = "italic", color = "grey30")
  )


}
