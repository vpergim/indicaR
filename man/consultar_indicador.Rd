% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consultar_indicador.R
\name{consultar_indicador}
\alias{consultar_indicador}
\title{Consultar un indicador}
\usage{
consultar_indicador(id_dataset, ...)
}
\arguments{
\item{id_dataset}{Cadena de texto con el identificador del indicador.
Los identificadores disponibles pueden consultarse con
\code{\link[=listar_indicadores]{listar_indicadores()}}.}

\item{...}{Filtros aplicados a las columnas del indicador. Cada filtro debe
indicarse como \code{columna = valor}. Se pueden proporcionar varios valores
mediante un vector, por ejemplo \code{anyo = c(2023, 2024)}.}
}
\value{
Un \code{data.frame} con las observaciones que cumplen los filtros.
}
\description{
Devuelve los datos de un indicador y permite filtrarlos por cualquiera
de las dimensiones disponibles en el dataset normalizado.
}
\details{
Los filtros se especifican mediante argumentos con nombre. El nombre debe
coincidir con una columna disponible en el indicador y los valores
solicitados deben existir en dicha columna.

Los nombres y valores de los filtros se validan explícitamente. Si se
solicita una columna inexistente o un valor que no está presente en el
indicador, la función genera un error en lugar de ignorarlo.

Cuando, después de aplicar los filtros solicitados, siguen presentes varios
tipos de territorio, debe indicarse explícitamente \code{tipo_territorio}.
Esto evita mezclar inadvertidamente niveles territoriales distintos.

Las dimensiones disponibles para cada indicador pueden consultarse con
\code{\link[=info_indicador]{info_indicador()}}.
}
\examples{
consultar_indicador(
  "TERRITORIO_SEXO_EDAD_A_01",
  anyo = 2024,
  sexo = "Mujeres",
  tipo_territorio = "ccaa"
)

}
\seealso{
\code{\link[=listar_indicadores]{listar_indicadores()}}, \code{\link[=info_indicador]{info_indicador()}}
}
